using UnityEngine;

public class PlayerHand : MonoBehaviour
{
    public static GameObject currentHeldObject;
    public static Transform holdPoint;   // ? New static reference

    // (keep any other code you already have)
}